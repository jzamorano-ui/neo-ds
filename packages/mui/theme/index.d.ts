// index.d.ts — GENERATED · theme types (`@neo/mui/theme`). Do NOT edit by hand.
// TypeScript only loads what it REACHES from the types entry: without this line, whoever enters through
// `@neo/mui/theme` does not get the augmentations, and `theme.neo.radius.md` or the `xxl` breakpoint do
// not compile.
/// <reference path="./neo.d.ts" />
import type { Theme, ThemeOptions } from '@mui/material/styles';
export declare const themeOptions: ThemeOptions;
export declare const theme: Theme;
export default theme;
